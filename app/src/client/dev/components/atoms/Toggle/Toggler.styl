.toggle {
  position: relative;
  padding: 10px;
  background-color: white;
  border-radius: 20px;
  width: 50%;
  margin: 10px 5px;
  display: flex;
  justify-content: space-around;
  font-size: 13px;
}