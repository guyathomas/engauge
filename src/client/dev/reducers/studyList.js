function studyList(state = [], action) {
  switch (action.type) {
    case 'SEARCH':
      return state;
    case 'TOGGLE_SHOW_ONLY_MINE':
      return state;
    default:
  }
  return state;
}

export default studyList;
