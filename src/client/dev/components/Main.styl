#app {
	display: flex;
	flex-direction: column;
}

body {
	margin: 0;
	font-family: 'Raleway', sans-serif;
}

a, a:visited, a:hover, a:active {
	color: inherit;
	text-decoration: none;
}

/* Global Variables */
header-height = 60px

/*Colors*/
dark-green = #003144

@import 'atoms/*'
@import 'molecules/*'
@import 'organisms/*'
@import 'pages/*'