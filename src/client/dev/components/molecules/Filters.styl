.filter-container {
	.filters {
		display: flex;
		flex-direction: row;
		justify-content: space-around;
		text-align: center;
	}
	.results {
	    text-align: center;
	    font-size: 13px;
	    color: #464C50;
	}
}