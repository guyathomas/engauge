machine:
  services:
    - docker

dependencies:
  override: 
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
    - docker build -t stolemyusername/engauged:latest .
    - docker push stolemyusername/engauged:latest


# test:
#   override:
#     - docker run -d -p 9200:9200 stolemyusername/engauge; sleep 10
#     - curl --retry 10 --retry-delay 5 -v http://localhost:9200

deployment:
  production:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      # - docker tag stolemyusername/engauged:latest registry.heroku.com/engauged/web:latest
      - docker images
      - docker push registry.heroku.com/engauged/web:latest
